<div class="content">
  <h1>Posts</h1>

  <% @posts.each do |post| %>
      <h2><%= post.user.email %></h2>
      <p><%= post.body %></p>
      <%= link_to 'Show', post %>
      <% if current_user == post.user %>
      <%= link_to 'Edit', edit_post_path(post) %>
      <% end %>
  <% end %>
</div>
